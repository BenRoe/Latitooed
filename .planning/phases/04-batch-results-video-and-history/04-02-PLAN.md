---
phase: 04-batch-results-video-and-history
plan: 04-02
type: execute
wave: 1
depends_on: []
files_modified:
  - GPSMetadataEditor/Features/MetadataWriting/ExifToolArgumentBuilder.swift
  - GPSMetadataEditor/Features/MetadataWriting/ExifToolMetadataWriter.swift
  - GPSMetadataEditorTests/ExifToolArgumentBuilderTests.swift
  - GPSMetadataEditorTests/ExifToolMetadataWriterTests.swift
autonomous: true
requirements:
  - META-03
  - META-04
must_haves:
  truths:
    - "D-12: Use the researched minimal MOV/MP4 ExifTool tag set, not a hard-coded context-only guess."
    - "D-13: If ExifTool exits cleanly for MOV or MP4, show success with `GPS metadata updated.`."
    - "D-14: Do not add extra compatibility caveats to successful video rows."
    - "D-15: MOV/MP4 rows show warnings or failures only when the helper reports warnings or failures."
    - "D-16: MOV and MP4 share the same user-facing behavior and copy by default."
    - "D-17: MOV and MP4 argument/result handling may split only if implementation evidence shows container-specific behavior."
  artifacts:
    - path: "GPSMetadataEditor/Features/MetadataWriting/ExifToolArgumentBuilder.swift"
      provides: "QuickTime Keys GPS arguments for MOV and MP4"
    - path: "GPSMetadataEditor/Features/MetadataWriting/ExifToolMetadataWriter.swift"
      provides: "video writes through the same bundled-helper process path as still images"
    - path: "GPSMetadataEditorTests/ExifToolArgumentBuilderTests.swift"
      provides: "exact video argument tests"
    - path: "GPSMetadataEditorTests/ExifToolMetadataWriterTests.swift"
      provides: "video result mapping tests"
  key_links:
    - from: "ExifToolArgumentBuilder.swift"
      to: "ExifToolMetadataWriter.swift"
      via: "gpsWriteArguments(for:coordinate:)"
      pattern: "-Keys:GPSCoordinates="
---

<objective>
Replace the Phase 3 MOV/MP4 warning path with best-effort QuickTime-compatible video GPS writes.

Purpose: Phase 4 must write location metadata for MOV and MP4 when ExifTool can do so, while keeping video support honest and best-effort.
Output: video ExifTool arguments, writer routing, success/warning/failure mapping, and tests proving MOV/MP4 invoke the bundled helper through argument arrays.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-batch-results-video-and-history/04-CONTEXT.md
@.planning/phases/04-batch-results-video-and-history/04-RESEARCH.md
@.planning/phases/04-batch-results-video-and-history/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add QuickTime Keys GPS arguments for MOV and MP4</name>
  <files>GPSMetadataEditor/Features/MetadataWriting/ExifToolArgumentBuilder.swift, GPSMetadataEditorTests/ExifToolArgumentBuilderTests.swift</files>
  <read_first>GPSMetadataEditor/Features/MetadataWriting/ExifToolArgumentBuilder.swift, GPSMetadataEditor/Features/FileIntake/Models/MediaFileKind.swift, GPSMetadataEditor/Features/CoordinateSelection/Models/CoordinateSelection.swift, GPSMetadataEditorTests/ExifToolArgumentBuilderTests.swift, .planning/phases/04-batch-results-video-and-history/04-RESEARCH.md</read_first>
  <behavior>
    - MOV arguments are exactly `-overwrite_original`, `-Keys:GPSCoordinates=<latitude>, <longitude>`, and the file path as its own final argument.
    - MP4 arguments use the same `Keys:GPSCoordinates` strategy.
    - Signed coordinates remain signed in the video GPS argument.
    - File paths with spaces, Unicode, and shell metacharacters remain one argument.
  </behavior>
  <action>Change `ExifToolArgumentBuilder.gpsWriteArguments(for:coordinate:)` so `.mov` and `.mp4` return ordered argument arrays using `-overwrite_original`, `-Keys:GPSCoordinates=\(coordinate.latitude), \(coordinate.longitude)`, and `file.url.path(percentEncoded: false)` as the final separate string. Remove or stop using `BuildError.unsupportedMediaKind` for MOV/MP4 if it becomes unreachable. Keep JPEG/HEIC behavior unchanged with `-gpsposition=`. Do not use unqualified `GPSCoordinates`, `QuickTime:GPSCoordinates`, shell quoting, `/bin/sh`, `/usr/bin/env`, `zsh`, PATH lookup, or new third-party code.</action>
  <verify><automated>xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test</automated></verify>
  <acceptance_criteria>
    - `ExifToolArgumentBuilderTests.swift` asserts MOV arguments contain `-Keys:GPSCoordinates=52.520008, 13.404954`.
    - `ExifToolArgumentBuilderTests.swift` asserts MP4 uses the same `-Keys:GPSCoordinates=` argument.
    - Existing JPEG/HEIC argument tests still expect `-gpsposition=`.
    - No metadata-writing source contains `/bin/sh`, `zsh`, `/usr/bin/env`, `ProcessInfo.processInfo.environment`, or an unqualified `-GPSCoordinates=`.
  </acceptance_criteria>
  <done>Video metadata writes have deterministic QuickTime Keys arguments.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route MOV and MP4 through the writer process path</name>
  <files>GPSMetadataEditor/Features/MetadataWriting/ExifToolMetadataWriter.swift, GPSMetadataEditorTests/ExifToolMetadataWriterTests.swift</files>
  <read_first>GPSMetadataEditor/Features/MetadataWriting/ExifToolMetadataWriter.swift, GPSMetadataEditor/Features/MetadataWriting/ExifToolArgumentBuilder.swift, GPSMetadataEditor/Features/MetadataWriting/ProcessRunner.swift, GPSMetadataEditor/Features/MetadataWriting/MetadataWriteResult.swift, GPSMetadataEditorTests/ExifToolMetadataWriterTests.swift, .planning/phases/04-batch-results-video-and-history/04-CONTEXT.md</read_first>
  <behavior>
    - MOV and MP4 invoke the injected `ProcessRunning` once when the helper resolves.
    - Clean exit status `0` maps to `.success`, `.updated`, and `GPS metadata updated.` for MOV and MP4.
    - Nonzero exit status maps to `.failure` with diagnostic detail.
    - Runner throws still map to `.failure` with `GPS metadata could not be written.`.
  </behavior>
  <action>Remove the Phase 3 `.mov/.mp4` warning branch from `ExifToolMetadataWriter.writeGPS`. Route JPEG, HEIC, MOV, and MP4 through a shared private write method that resolves the bundled helper, builds arguments, starts security-scoped access, awaits `processRunner.run(executableURL:arguments:)`, captures diagnostics, and maps termination status. Keep success user copy exactly `GPS metadata updated.` and do not append video compatibility caveats. Show warnings only if implementation adds a tested helper-warning classification; otherwise nonzero helper outcomes are failures. Do not introduce cancellation UI, per-kind user caveat copy, or a system ExifTool fallback.</action>
  <verify><automated>xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test</automated></verify>
  <acceptance_criteria>
    - `ExifToolMetadataWriterTests.swift` no longer contains a test named or described as `Videos return warnings without invoking the runner`.
    - Tests assert MOV and MP4 success invoke the runner and return message `GPS metadata updated.`.
    - Tests assert MOV/MP4 nonzero exit includes `Exit status: 1` or equivalent diagnostic detail.
    - `ExifToolMetadataWriter.swift` no longer returns `Video metadata writing is deferred to Phase 4.`.
  </acceptance_criteria>
  <done>MOV and MP4 use the real bundled-helper write path with still-image-style result mapping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File path to helper arguments | User-selected MOV/MP4 paths may contain shell metacharacters. |
| Helper output to app result state | ExifTool stdout/stderr controls diagnostics but not primary success copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering/Elevation | video argument construction | mitigate | Keep every flag and file path as a separate `[String]` argument; never build shell commands. |
| T-04-02-02 | Spoofing/Tampering | helper resolution | mitigate | Continue resolving only bundled ExifTool; no Homebrew/system fallback. |
| T-04-02-03 | Information Integrity | video compatibility | mitigate | Clean helper exit maps to app-owned success copy; helper warnings/failures drive warnings/failures only when reported. |
| T-04-02-SC | Supply chain | dependencies | accept | No new third-party Swift framework is introduced. |
</threat_model>

<verification>
Run `xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test`.

Host smoke after unit tests: apply Berlin GPS to one sample MOV and one sample MP4, then read back with bundled ExifTool using `-a -G1 -s -n "-GPS*"` or equivalent and verify the app shows either success or a clear helper-derived failure/warning.
</verification>

<success_criteria>
- META-03 and META-04 are satisfied by best-effort `Keys:GPSCoordinates` writes for MOV and MP4.
- Argument-array safety remains preserved by keeping every ExifTool flag and file path as a separate process argument.
- Structured result behavior remains preserved by mapping clean exits, nonzero exits, and thrown process errors into `MetadataWriteResult`.
</success_criteria>

<output>
Create `.planning/phases/04-batch-results-video-and-history/04-02-SUMMARY.md` when done.
</output>
