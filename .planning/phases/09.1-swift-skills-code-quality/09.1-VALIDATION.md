---
phase: 09.1
slug: swift-skills-code-quality
status: draft
nyquist_compliant: false
wave_0_complete: true
created: 2026-05-28
---

# Phase 09.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Swift Testing (Swift 6.2, bundled with Xcode) |
| **Config file** | none — targets in GPSMetadataEditor.xcodeproj |
| **Quick run command** | `xcodebuild test -scheme GPSMetadataEditor -destination 'platform=macOS' 2>&1 | grep -E "Test.*passed|Test.*failed|error:"` |
| **Full suite command** | `xcodebuild test -scheme GPSMetadataEditor -destination 'platform=macOS'` |
| **Build-only check** | `xcodebuild build -scheme GPSMetadataEditor -destination 'platform=macOS' 2>&1 | grep -E "error:|BUILD"` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run build-only check
- **After every plan wave:** Run full suite
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Area | Automated Command | File Exists | Status |
|---------|------|------|------|-------------------|-------------|--------|
| 09.1-01 T1 | 01 | 1 | CONC-HIGH | `xcodebuild test` — FileIntakeViewModelTests | ✅ (extend) | ⬜ pending |
| 09.1-01 T2 | 01 | 1 | TEST-HIGH + TEST-MEDIUM + LOW | `xcodebuild test` — ProcessRunnerTests, CoordinateSelectionViewModelTests, FileIntakeViewModelTests, MetadataBatchViewModelTests | ✅ (patch) | ⬜ pending |
| 09.1-02 T1 | 02 | 1 | DATA-HIGH | `xcodebuild build` | manual build | ⬜ pending |
| 09.1-02 T2 | 02 | 1 | DATA-MEDIUM + DATA-LOW | `xcodebuild test` — BatchHistoryStoreTests | ✅ (verify) | ⬜ pending |
| 09.1-02 T3 | 02 | 1 | DATA-MEDIUM + DATA-LOW | `xcodebuild test` + grep for existingRecentCoordinate | ✅ (verify) | ⬜ pending |
| 09.1-03 T1 | 03 | 2 | UI-P1 | `xcodebuild build` + grep for accessibilityAddTraits, ContentUnavailableView | manual VoiceOver | ⬜ pending |
| 09.1-03 T2 | 03 | 2 | UI-P2 | `xcodebuild build` + grep for smallSize/mediumSize/largeSize (must = 0) | build gate | ⬜ pending |
| 09.1-03 T3 | 03 | 2 | UI-P2 + UI-LOW | `xcodebuild build` + grep for GeometryReader in CoordinateSearchPanel (must = 0) + grep for filter.*count in FileIntakeViewModel (must = 0) | build gate | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. No new test files or framework installs needed.

All automated checks use `xcodebuild test` (existing suite) or `xcodebuild build` (compile gate).

---

## Manual-Only Verifications

| Behavior | Area | Why Manual | Test Instructions |
|----------|------|------------|-------------------|
| Map tap gesture announced as button with label | UI-P1 | VoiceOver on real macOS | Enable VoiceOver, hover over map, verify "Map. Tap to set GPS coordinate. Button" is read |
| No double announcement in RecentCoordinateRow | UI-P1 | VoiceOver on real macOS | Enable VoiceOver, hover over a recent coordinate row, verify label read once |
| ContentUnavailableView renders correctly | UI-P1 | Visual | Launch app with no files loaded, verify empty state shows icon + title |
| onGeometryChange dropdown positioning correct | UI-P2 | Visual | Type in search field, verify dropdown anchors at correct Y offset |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or build-gate check
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references — N/A (existing infrastructure)
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter when sign-off complete

**Approval:** pending
