---
phase: 06-add-a-grid-view-for-loaded-files
plan: 06-03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - GPSMetadataEditor/Features/FileIntake/Views/SelectedFilesGrid.swift
  - GPSMetadataEditor/Features/FileIntake/FileIntakeViewModel.swift
  - GPSMetadataEditorTests/FileIntakeViewModelTests.swift
  - .planning/phases/06-add-a-grid-view-for-loaded-files/06-HUMAN-UAT.md
autonomous: false
requirements:
  - GRID-03
  - GRID-04
  - GRID-05
must_haves:
  truths:
    - "D-11: Grid mode mirrors table multi-selection through the shared `selectedFileIDs` set."
    - "D-12: Plain-clicking a grid card replaces selection with that card."
    - "D-13: Command-click toggles grid-card selection, and Shift-click range-selects when clean."
    - "D-15: The grid must not break existing SwiftUI `Table` multi-selection behavior."
    - "D-17: Thumbnail generation is best effort; fallback icons remain useful."
    - "D-18: The grid remains usable for small and large batches without disrupting warning visibility or batch actions."
  artifacts:
    - path: "GPSMetadataEditor/Features/FileIntake/Views/SelectedFilesGrid.swift"
      provides: "grid selection behavior, accessibility labels, and fallback-first card states"
    - path: "GPSMetadataEditorTests/FileIntakeViewModelTests.swift"
      provides: "selection helper coverage for replace, toggle, and range-select behavior"
    - path: ".planning/phases/06-add-a-grid-view-for-loaded-files/06-HUMAN-UAT.md"
      provides: "host-side manual smoke checklist for grid/table behavior"
  key_links:
    - from: "SelectedFilesGrid.swift"
      to: "SelectedFilesTable.swift"
      via: "shared `selectedFileIDs` binding"
      pattern: "grid selection changes must not modify table internals"
---

<objective>
Harden grid selection behavior, accessibility, and host verification.

Purpose: Phase 6 is not complete until grid cards update the same detail/diagnostics state as the table and the existing table workflow remains intact.
Output: grid plain-click, Command-click, optional clean Shift-click range selection, accessibility/source checks, and a host UAT checklist.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-add-a-grid-view-for-loaded-files/06-CONTEXT.md
@.planning/phases/06-add-a-grid-view-for-loaded-files/06-RESEARCH.md
@.planning/phases/06-add-a-grid-view-for-loaded-files/06-PATTERNS.md
@docs/swiftui-table-selection-behavior.md
@docs/host-xcodebuild-verification-boundary.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reusable grid selection helpers</name>
  <files>GPSMetadataEditor/Features/FileIntake/FileIntakeViewModel.swift, GPSMetadataEditorTests/FileIntakeViewModelTests.swift</files>
  <read_first>GPSMetadataEditor/Features/FileIntake/FileIntakeViewModel.swift, GPSMetadataEditor/Features/FileIntake/Models/SelectedMediaFile.swift, GPSMetadataEditorTests/FileIntakeViewModelTests.swift, .planning/phases/06-add-a-grid-view-for-loaded-files/06-CONTEXT.md, .agents/skills/swift-testing-pro/SKILL.md</read_first>
  <behavior>
    - Plain grid selection replaces selection with the clicked file ID.
    - Command-click toggles the clicked file ID in `selectedFileIDs`.
    - Shift-click range selection selects the contiguous range between an anchor ID and clicked ID when both IDs exist in `selectedFiles`.
    - If Shift-click cannot be implemented cleanly, the code leaves a small explicit fallback that behaves like plain-click and the UAT checklist records Shift-click as not implemented.
  </behavior>
  <action>Add small main-actor helper methods on `FileIntakeViewModel` or a nested value helper for grid selection: `replaceGridSelection(with:)`, `toggleGridSelection(id:)`, and `selectGridRange(to:)` or equivalent concrete names. Track a session-only `lastGridSelectionAnchorID` if needed for range selection. Range selection must derive order from `selectedFiles` and ignore stale IDs. Add Swift Testing coverage for replace, toggle on, toggle off, range from first to third file, and stale/missing range fallback. Do not change `SelectedFilesTable`, `TableSelectionNormalizer`, or table-specific AppKit event monitoring.</action>
  <verify><automated>xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test</automated></verify>
  <acceptance_criteria>
    - A test proves plain grid selection sets `selectedFileIDs == [clicked.id]`.
    - A test proves command toggle adds a second ID without dropping the first.
    - A test proves command toggle removes an already selected ID.
    - A test proves range selection selects IDs in `selectedFiles` order when anchor and clicked IDs exist, or the plan summary explicitly documents why Shift-click was left as plain-click fallback.
    - `SelectedFilesTable.swift` is not modified by this task except for unrelated user changes that pre-existed execution.
  </acceptance_criteria>
  <done>Grid selection behavior is deterministic and covered by view-model tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire modifier-aware grid card activation and accessibility labels</name>
  <files>GPSMetadataEditor/Features/FileIntake/Views/SelectedFilesGrid.swift</files>
  <read_first>GPSMetadataEditor/Features/FileIntake/Views/SelectedFilesGrid.swift, GPSMetadataEditor/Features/FileIntake/FileIntakeViewModel.swift, GPSMetadataEditor/Features/FileIntake/Views/SelectedFilesTable.swift, docs/swiftui-table-selection-behavior.md, .planning/phases/06-add-a-grid-view-for-loaded-files/06-CONTEXT.md, .agents/skills/swiftui-pro/SKILL.md, .agents/skills/swift-concurrency-pro/SKILL.md</read_first>
  <behavior>
    - Plain clicking a card replaces selection.
    - Command-click toggles selection.
    - Shift-click range-selects if Task 1 implemented range selection cleanly.
    - Selected cards have a visible selected state and accessible selected-state label.
    - Card accessibility includes filename, file type, GPS status, and latest result.
  </behavior>
  <action>Update `SelectedFilesGrid` to route card activation through the helpers from Task 1. Use `Button` for the primary card activation where practical. If modifier-key handling requires a minimal AppKit event bridge or SwiftUI environment event check, isolate it to the grid and keep it out of `SelectedFilesTable`. Add selected-card styling using existing adaptive colors, `foregroundStyle`, and a text/icon selected indicator where needed. Add `.accessibilityLabel` or equivalent so VoiceOver can hear filename, file type, GPS status, latest result, and whether the card is selected. Do not add `onTapGesture`, per-file unstructured thumbnail tasks, fixed font sizes, UIKit colors, or third-party libraries.</action>
  <verify><automated>xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test</automated></verify>
  <acceptance_criteria>
    - `SelectedFilesGrid.swift` routes plain, command, and shift selection to named helpers or documented fallback behavior.
    - `SelectedFilesGrid.swift` contains accessibility text for file type, GPS status, latest result, and selected state.
    - Selected-card styling uses text/icon or shape in addition to color.
    - `rg -n "onTapGesture|foregroundColor|cornerRadius\\(|AnyView|font\\(\\.system|Task \\{" GPSMetadataEditor/Features/FileIntake/Views/SelectedFilesGrid.swift` returns no matches.
    - `SelectedFilesTable.swift` still contains `Table(files, selection: $selection)`.
  </acceptance_criteria>
  <done>Grid cards update shared selection and remain accessible without table-selection regressions.</done>
</task>

<task type="manual" tdd="false">
  <name>Task 3: Create Phase 6 host UAT checklist and run verification</name>
  <files>.planning/phases/06-add-a-grid-view-for-loaded-files/06-HUMAN-UAT.md</files>
  <read_first>.planning/phases/06-add-a-grid-view-for-loaded-files/06-CONTEXT.md, .planning/phases/06-add-a-grid-view-for-loaded-files/06-RESEARCH.md, docs/host-xcodebuild-verification-boundary.md, docs/swiftui-table-selection-behavior.md, .planning/phases/05-packaging-and-release-verification/05-HUMAN-UAT.md</read_first>
  <behavior>
    - The checklist separates VM/source checks from host Xcode/app checks.
    - Host checks cover loading JPEG, HEIC, MOV, and MP4 files.
    - Host checks cover grid default, Table/Grid switching, table multi-select, grid plain-click, grid Command-click, grid Shift-click if implemented, detail summary, warnings/results, and Apply Location.
    - The checklist starts with pending result rows and is not marked passed before host evidence exists.
  </behavior>
  <action>Create `.planning/phases/06-add-a-grid-view-for-loaded-files/06-HUMAN-UAT.md` with frontmatter `phase: 06-add-a-grid-view-for-loaded-files`, `status: pending`, and `created: 2026-05-24`. Include exact host command `xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test`. Include manual smoke rows for: load mixed media; grid is default after files load; Table/Grid switch preserves loaded files; table Command-click multi-select still works; grid plain-click replaces selection; grid Command-click toggles selection; grid Shift-click range-selects or is recorded as intentionally not implemented; multi-selection shows summary; one selected warning/failure row still shows diagnostics; Apply Location remains enabled when coordinate and files exist; unsupported/inaccessible warnings remain visible. Leave every result as `[pending]`.</action>
  <verify><manual>Run `rg -n "xcodebuild|grid is default|Command-click|Shift-click|Apply Location|\\[pending\\]" .planning/phases/06-add-a-grid-view-for-loaded-files/06-HUMAN-UAT.md`.</manual></verify>
  <acceptance_criteria>
    - `06-HUMAN-UAT.md` exists.
    - `06-HUMAN-UAT.md` contains the exact host `xcodebuild` test command.
    - `06-HUMAN-UAT.md` contains rows for grid default, Table/Grid switch, table Command-click, grid plain-click, grid Command-click, and grid Shift-click.
    - `06-HUMAN-UAT.md` contains `Apply Location`.
    - `06-HUMAN-UAT.md` contains `result: [pending]` and does not contain `status: passed`.
  </acceptance_criteria>
  <done>Phase 6 has a concrete host verification checklist for final acceptance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input events to selection state | Mouse modifiers determine grid selection changes. |
| Grid review surface to batch action | The grid is a review surface; existing Apply Location behavior remains batch-wide over loaded files. |
| VM verification to host UI truth | SwiftUI/AppKit interaction behavior requires host Xcode/app smoke evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | selection state | mitigate | Keep selection helper logic deterministic and tested against stale IDs and ordered ranges. |
| T-06-03-02 | Spoofing | selected visual state | mitigate | Render selected state with accessible text/icon or shape, not color alone. |
| T-06-03-03 | Denial of Service | large selection batches | mitigate | Keep selection operations over the loaded `selectedFiles` array and avoid background task fan-out. |
| T-06-03-04 | Repudiation | host verification | mitigate | Capture pending UAT rows with exact checks and require host evidence before marking passed. |
| T-06-03-SC | Supply chain | dependencies | accept | No third-party selection, grid, or image-loading library is introduced. |
</threat_model>

<verification>
VM/source checks:
- `xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test`
- `rg -n "LazyVGrid|GridItem\\(\\.adaptive|pickerStyle\\(\\.segmented\\)|SelectedFilesGrid" GPSMetadataEditor GPSMetadataEditor.xcodeproj/project.pbxproj`
- `rg -n "onTapGesture|foregroundColor|cornerRadius\\(|AnyView|font\\(\\.system|Task \\{" GPSMetadataEditor/Features/FileIntake/Views/SelectedFilesGrid.swift`
- `rg -n "Table\\(files, selection: \\$selection\\)" GPSMetadataEditor/Features/FileIntake/Views/SelectedFilesTable.swift`

Host manual checks:
- Launch the app on macOS/Xcode.
- Load JPEG, HEIC, MOV, and MP4 files.
- Confirm grid is default after files load.
- Switch Table/Grid and confirm loaded files, selection, detail, warning summary, batch history, footer, and Apply Location remain coherent.
- Confirm grid plain-click, Command-click, and Shift-click behavior according to `06-HUMAN-UAT.md`.
</verification>

<success_criteria>
- GRID-03 is satisfied by grid selection updating the same detail/diagnostics state as the table.
- GRID-04 is satisfied by source checks plus host UAT rows proving table workflow, warnings, and batch actions remain intact.
- GRID-05 is satisfied by accessible, text-backed visual distinctions for unsupported/fallback, pending, success, warning, and failure states.
</success_criteria>

<output>
Create `.planning/phases/06-add-a-grid-view-for-loaded-files/06-03-SUMMARY.md` when done.
</output>
