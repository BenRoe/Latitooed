---
phase: 05-packaging-and-release-verification
plan: 05-02
type: execute
wave: 1
depends_on: []
files_modified:
  - GPSMetadataEditorTests/ExifToolMetadataWriterTests.swift
  - GPSMetadataEditorTests/ExifToolArgumentBuilderTests.swift
autonomous: true
requirements:
  - PKG-02
  - PKG-03
must_haves:
  truths:
    - "D-04: Verification includes a negative external-helper proof with Homebrew/system ExifTool unavailable or PATH stripped."
    - "D-05: Do not rename or mutate host Homebrew/system ExifTool."
    - "D-08: Build and packaging use bundled repo resources; avoid adding a Homebrew ExifTool build dependency."
    - "D-16: Missing-helper, non-executable-helper, and launch-failure behavior should be covered with unit or integration tests."
    - "D-17: Manual packaged-app verification only needs the happy path for helper execution and JPEG/HEIC writes."
    - "D-18: Do not require manual destructive variants that remove or chmod the packaged helper."
  artifacts:
    - path: "GPSMetadataEditorTests/ExifToolMetadataWriterTests.swift"
      provides: "helper failure and bundle-only writer tests"
    - path: "GPSMetadataEditorTests/ExifToolArgumentBuilderTests.swift"
      provides: "argument array path safety tests"
  key_links:
    - from: "BundledExifToolResolver.swift"
      to: "ExifToolMetadataWriterTests.swift"
      via: "typed helper errors mapped to user-facing messages"
      pattern: "Bundled ExifTool helper is missing."
    - from: "ExifToolMetadataWriter.swift"
      to: "FileIntakeView.confirmOverwrite"
      via: "packaged happy path uses normal app writer"
      pattern: "ExifToolMetadataWriter()"
---

<objective>
Tighten no-fallback and helper-failure test coverage without changing the metadata-writing architecture.

Purpose: Phase 5 must prove signed app helper execution is bundle-only and helper failures remain clear, while avoiding invasive manual app-bundle mutation tests.
Output: targeted Swift Testing coverage for bundle-only resolution, process-launch failure mapping, and argument-path safety that supports PKG-02 and PKG-03.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-packaging-and-release-verification/05-CONTEXT.md
@.planning/phases/05-packaging-and-release-verification/05-RESEARCH.md
@.planning/phases/05-packaging-and-release-verification/05-PATTERNS.md
@GPSMetadataEditor/Features/MetadataWriting/BundledExifToolResolver.swift
@GPSMetadataEditor/Features/MetadataWriting/ExifToolMetadataWriter.swift
@GPSMetadataEditor/Features/MetadataWriting/ProcessRunner.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit no-PATH resolver and writer assertions</name>
  <files>GPSMetadataEditorTests/ExifToolMetadataWriterTests.swift</files>
  <read_first>GPSMetadataEditorTests/ExifToolMetadataWriterTests.swift, GPSMetadataEditor/Features/MetadataWriting/BundledExifToolResolver.swift, GPSMetadataEditor/Features/MetadataWriting/ExifToolMetadataWriter.swift, .planning/phases/05-packaging-and-release-verification/05-CONTEXT.md</read_first>
  <behavior>
    - Tests prove resolver output comes from the supplied bundle path and not a system path.
    - Tests prove writer failure messages remain user-facing for missing and non-executable helpers.
    - Tests avoid mutating a real packaged app bundle.
  </behavior>
  <action>Extend existing `ExifToolMetadataWriterTests` if needed with assertions that `BundledExifToolResolver(bundle:)` returns a path under the supplied temporary bundle and not `/opt/homebrew/bin/exiftool`, `/usr/local/bin/exiftool`, `/usr/bin/exiftool`, or `/usr/bin/env`. Keep existing missing-helper and non-executable-helper assertions. Do not add production fallback code, environment-variable lookup, or package-time Homebrew dependency.</action>
  <verify><automated>xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test</automated></verify>
  <acceptance_criteria>
    - `ExifToolMetadataWriterTests.swift` asserts resolved helper path contains `/ExifTool/exiftool`.
    - `ExifToolMetadataWriterTests.swift` asserts resolved helper path does not contain `/opt/homebrew`, `/usr/local/bin`, or `/usr/bin/env`.
    - `BundledExifToolResolver.swift` contains no `/opt/homebrew`, `/usr/local`, `/usr/bin/env`, `ProcessInfo.processInfo.environment`, or `PATH`.
    - Missing-helper and non-executable-helper tests still assert `Bundled ExifTool helper is missing.` and `Bundled ExifTool helper is not executable.`.
  </acceptance_criteria>
  <done>Resolver/writer tests make bundle-only behavior explicit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confirm process launch failure maps to clear metadata failure</name>
  <files>GPSMetadataEditorTests/ExifToolMetadataWriterTests.swift</files>
  <read_first>GPSMetadataEditorTests/ExifToolMetadataWriterTests.swift, GPSMetadataEditor/Features/MetadataWriting/ExifToolMetadataWriter.swift, GPSMetadataEditor/Features/MetadataWriting/ProcessRunner.swift, .agents/skills/swift-concurrency-pro/references/bridging.md</read_first>
  <behavior>
    - Process-runner launch failure maps to `.failure`.
    - User-facing message is `GPS metadata could not be written.` for process failure after helper resolution.
    - Diagnostic detail preserves the localized failure text.
  </behavior>
  <action>Review the existing `runnerThrowMapsToStructuredFailure` test. If it already covers launch/process failure clearly, leave it intact and add only a comment or test name adjustment if needed. If coverage is ambiguous, add a focused throwing fake runner test for a JPEG file that expects `.failure`, message `GPS metadata could not be written.`, and diagnostic detail containing the fake localized error. Do not launch a real missing executable from unit tests.</action>
  <verify><automated>xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test</automated></verify>
  <acceptance_criteria>
    - `ExifToolMetadataWriterTests.swift` contains a process-runner throwing test for a still-image file or existing equivalent coverage.
    - That test asserts status `.failure`.
    - That test asserts message `GPS metadata could not be written.`.
    - That test asserts diagnostic detail contains the fake runner error string.
  </acceptance_criteria>
  <done>Helper launch/process failure behavior remains clear without manual package mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Keep GPS write arguments independent of shell and PATH</name>
  <files>GPSMetadataEditorTests/ExifToolArgumentBuilderTests.swift</files>
  <read_first>GPSMetadataEditorTests/ExifToolArgumentBuilderTests.swift, GPSMetadataEditor/Features/MetadataWriting/ExifToolArgumentBuilder.swift, .planning/phases/05-packaging-and-release-verification/05-RESEARCH.md</read_first>
  <behavior>
    - Argument arrays contain metadata tags and final file path only.
    - Argument builder never inserts executable name, shell path, PATH command, or Homebrew path.
    - JPEG and HEIC paths with shell metacharacters remain one argument.
  </behavior>
  <action>Review existing `ExifToolArgumentBuilderTests` for path-safety coverage. Add assertions if missing that JPEG and HEIC argument arrays do not contain `exiftool`, `/opt/homebrew/bin/exiftool`, `/usr/local/bin/exiftool`, `/bin/sh`, `zsh`, `/usr/bin/env`, or `PATH`. Preserve current exact argument order for still images: `-overwrite_original`, `-gpsposition=<lat>, <lon>`, final file path.</action>
  <verify><automated>xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test</automated></verify>
  <acceptance_criteria>
    - `ExifToolArgumentBuilderTests.swift` asserts the final path remains one separate argument for shell-metacharacter paths.
    - `ExifToolArgumentBuilderTests.swift` or source checks prove builder arguments do not include `/opt/homebrew`, `/usr/local/bin`, `/bin/sh`, `zsh`, `/usr/bin/env`, or `PATH`.
    - `ExifToolArgumentBuilder.swift` contains no executable-path lookup or process-launch code.
  </acceptance_criteria>
  <done>Argument construction cannot accidentally become a shell/system-exiftool fallback path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bundle resolver to helper execution | Resolver path determines which executable handles user files. |
| Unit tests to packaged behavior | Unit tests prove failure semantics but do not replace host packaged smoke. |
| User-selected path to subprocess arguments | File paths must remain arguments, not shell text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Spoofing | helper resolver | mitigate | Assert resolver paths are bundle-local and not Homebrew/system/env paths. |
| T-05-02-02 | Tampering | subprocess arguments | mitigate | Keep executable path out of argument builder and preserve file path as one argument. |
| T-05-02-03 | Repudiation | helper launch failure | mitigate | Preserve failure diagnostic text in structured result. |
| T-05-02-04 | Scope creep | manual failure variants | mitigate | Keep missing/non-executable/launch-failure coverage in tests; package smoke remains happy path only. |
</threat_model>

<verification>
Run host:

`xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test`

VM fallback checks:

- `rg -n "/opt/homebrew|/usr/local/bin|/usr/bin/env|PATH|/bin/sh|zsh" GPSMetadataEditor/Features/MetadataWriting GPSMetadataEditorTests/ExifToolMetadataWriterTests.swift GPSMetadataEditorTests/ExifToolArgumentBuilderTests.swift`
- Review any matches manually; expected production metadata-writing source should not contain fallback lookup paths.
</verification>

<success_criteria>
- PKG-02 is covered by bundle-only resolver and no PATH/Homebrew argument path.
- PKG-03 is covered by missing/non-executable/launch-failure mapping tests.
- D-04, D-05, D-08, D-16, D-17, and D-18 are visible in plan `must_haves` and task criteria.
</success_criteria>

<output>
Create `.planning/phases/05-packaging-and-release-verification/05-02-SUMMARY.md` when done.
</output>
