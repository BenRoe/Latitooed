---
phase: 01-app-shell-and-file-intake
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - GPSMetadataEditor.xcodeproj/project.pbxproj
  - GPSMetadataEditor/GPSMetadataEditorApp.swift
  - GPSMetadataEditor/Features/FileIntake/FileIntakeView.swift
  - GPSMetadataEditor/Support/AppDesign.swift
  - GPSMetadataEditorTests/FileIntakeSmokeTests.swift
autonomous: true
requirements:
  - FILE-01
must_haves:
  truths:
    - "D-01: User launches a native macOS SwiftUI utility and sees an obvious file-intake starting point."
    - "D-03: Main window reserves a quiet right-side region for Phase 2 without exposing coordinate controls."
    - "D-04: The right-side Phase 1 reserved panel stays visually quiet and cannot be mistaken for active coordinate selection."
    - "D-05: A small footer/status area exists for neutral Phase 1 status without shifting focus away from file intake."
  artifacts:
    - path: "GPSMetadataEditor.xcodeproj/project.pbxproj"
      provides: "macOS app target and Swift Testing test target"
    - path: "GPSMetadataEditor/GPSMetadataEditorApp.swift"
      provides: "SwiftUI app entry point"
    - path: "GPSMetadataEditor/Features/FileIntake/FileIntakeView.swift"
      provides: "main split file-intake shell"
  key_links:
    - from: "GPSMetadataEditor/GPSMetadataEditorApp.swift"
      to: "GPSMetadataEditor/Features/FileIntake/FileIntakeView.swift"
      via: "WindowGroup root view"
      pattern: "FileIntakeView\\("
    - from: "GPSMetadataEditorTests/FileIntakeSmokeTests.swift"
      to: "GPSMetadataEditor/Features/FileIntake/FileIntakeView.swift"
      via: "test imports app module"
      pattern: "@testable import GPSMetadataEditor"
---

<objective>
Create the native macOS SwiftUI app shell and first launch file-intake surface.

Purpose: Phase 1 needs a real app window before file intake logic can be exercised.
Output: Xcode project, app entry point, root file-intake view, shared design constants, and a smoke test target.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-app-shell-and-file-intake/01-CONTEXT.md
@.planning/phases/01-app-shell-and-file-intake/01-RESEARCH.md
@.planning/phases/01-app-shell-and-file-intake/01-UI-SPEC.md
@.planning/phases/01-app-shell-and-file-intake/01-PATTERNS.md
@.planning/phases/01-app-shell-and-file-intake/01-SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the macOS SwiftUI project skeleton</name>
  <files>GPSMetadataEditor.xcodeproj/project.pbxproj, GPSMetadataEditor/GPSMetadataEditorApp.swift, GPSMetadataEditorTests/FileIntakeSmokeTests.swift</files>
  <read_first>AGENTS.md, .planning/phases/01-app-shell-and-file-intake/01-RESEARCH.md, .planning/phases/01-app-shell-and-file-intake/01-PATTERNS.md</read_first>
  <action>Create a macOS SwiftUI app target named `GPSMetadataEditor` and a Swift Testing test target named `GPSMetadataEditorTests`. Set deployment to macOS 26.0 or later, Swift language mode to Swift 6.2 or later where Xcode project settings support it, and strict concurrency-friendly settings. Create `GPSMetadataEditorApp` as the `@main` app entry with a single `WindowGroup` whose root is `FileIntakeView()`. Use separate Swift files for separate types. Do not add third-party frameworks, UIKit, AppKit view wrappers, SwiftData, metadata writing, MapKit UI, or batch execution.</action>
  <verify><automated>xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' build</automated></verify>
  <done>`GPSMetadataEditor.xcodeproj/project.pbxproj` defines app and test targets; `GPSMetadataEditorApp.swift` contains `@main` and `FileIntakeView()`; `xcodebuild ... build` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Render the split first-launch intake shell</name>
  <files>GPSMetadataEditor/Features/FileIntake/FileIntakeView.swift, GPSMetadataEditor/Support/AppDesign.swift, GPSMetadataEditorTests/FileIntakeSmokeTests.swift</files>
  <read_first>GPSMetadataEditor/GPSMetadataEditorApp.swift, .planning/phases/01-app-shell-and-file-intake/01-CONTEXT.md, .planning/phases/01-app-shell-and-file-intake/01-UI-SPEC.md</read_first>
  <action>Create `AppDesign` with spacing and semantic design constants from `01-UI-SPEC.md`, expressed for SwiftUI usage without fixed font-size modifiers in view code. Implement `FileIntakeView` as a native split utility layout: left side has the D-01 drop-zone-first launch surface with an `Add Files` labeled button and copy from the UI-SPEC; right side is the D-03/D-04 quiet reserved location panel with the exact heading `Location selection comes next` and no map, coordinate, metadata, or batch controls; include a D-05 low-emphasis footer/status region. Use `foregroundStyle()`, `clipShape(.rect(cornerRadius:))`, `Label` or labeled `Button`, and Dynamic Type-compatible text styles.</action>
  <verify><automated>xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test</automated></verify>
  <done>`FileIntakeView.swift` contains visible copy `Drop media files here`, `Add Files`, and `Location selection comes next`; there is no `Map(`, latitude/longitude text field, metadata writer, batch button, `foregroundColor`, `cornerRadius`, `onTapGesture`, `ObservableObject`, `@Published`, `@StateObject`, or `@ObservedObject` in the created app source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User desktop to app process | The app opens local files selected by the user in later plans; file URLs are untrusted input. |
| Source repository to build system | New Xcode project settings define what code builds and tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | Xcode project settings | mitigate | Keep app/test targets local to this repo, do not add third-party frameworks, and verify with `xcodebuild`. |
| T-01-01-02 | Spoofing | Reserved location panel | mitigate | Render neutral Phase 1 copy only so users cannot mistake unavailable coordinate selection for active functionality. |
| T-01-01-SC | Tampering | package installs | accept | No npm, pip, cargo, or third-party Swift package installs are planned. |
</threat_model>

<verification>
Run `xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test`.
</verification>

<success_criteria>
- FILE-01 has a launchable macOS app shell with an `Add Files` action ready for picker wiring.
- D-01, D-03, D-04, and D-05 are represented in the visible shell.
- The right-side panel contains no Phase 2 controls.
</success_criteria>

<output>
Create `.planning/phases/01-app-shell-and-file-intake/01-01-SUMMARY.md` when done.
</output>
