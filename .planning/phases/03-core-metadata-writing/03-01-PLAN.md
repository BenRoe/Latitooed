---
phase: 03-core-metadata-writing
plan: 03-01
type: execute
wave: 1
depends_on: []
files_modified:
  - GPSMetadataEditor.xcodeproj/project.pbxproj
  - GPSMetadataEditor/Features/MetadataWriting/MetadataWriter.swift
  - GPSMetadataEditor/Features/MetadataWriting/MetadataWriteResult.swift
  - GPSMetadataEditor/Features/MetadataWriting/ExifToolArgumentBuilder.swift
  - GPSMetadataEditorTests/ExifToolArgumentBuilderTests.swift
autonomous: true
requirements:
  - BATCH-05
  - META-01
  - META-02
  - META-06
  - META-07
must_haves:
  truths:
    - "D-01: Phase 3 overwrites metadata in place and does not create ExifTool `_original` backups."
    - "D-07: ExifTool invocation must use an executable URL and argument array, not shell command strings."
    - "D-08: Argument construction is isolated enough for unit tests to verify without launching ExifTool."
    - "D-09: Phase 3 writes JPEG and HEIC files only."
    - "META-07: Results include status, user-facing message, and diagnostic detail when available."
  artifacts:
    - path: "GPSMetadataEditor/Features/MetadataWriting/MetadataWriter.swift"
      provides: "fakeable async metadata writer protocol"
    - path: "GPSMetadataEditor/Features/MetadataWriting/MetadataWriteResult.swift"
      provides: "structured per-file result values"
    - path: "GPSMetadataEditor/Features/MetadataWriting/ExifToolArgumentBuilder.swift"
      provides: "unit-testable GPS write argument array"
    - path: "GPSMetadataEditorTests/ExifToolArgumentBuilderTests.swift"
      provides: "argument order, overwrite, GPSPosition, and path safety tests"
  key_links:
    - from: "ExifToolArgumentBuilder.swift"
      to: "CoordinateSelection.swift"
      via: "latitude/longitude source"
      pattern: "CoordinateSelection"
    - from: "MetadataWriteResult.swift"
      to: "FileResultStatus.swift"
      via: "result status mapping"
      pattern: "FileResultStatus"
---

<objective>
Create the metadata-writing service contract and pure ExifTool GPS argument builder.

Purpose: Phase 3 needs a narrow, testable service boundary before any subprocess code or UI command is wired in.
Output: `MetadataWriter`, structured result values, `ExifToolArgumentBuilder`, project membership, and Swift Testing coverage that proves destructive still-image arguments are built as an argument array.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-core-metadata-writing/03-CONTEXT.md
@.planning/phases/03-core-metadata-writing/03-RESEARCH.md
@.planning/phases/03-core-metadata-writing/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add metadata writer protocol and result values</name>
  <files>GPSMetadataEditor/Features/MetadataWriting/MetadataWriter.swift, GPSMetadataEditor/Features/MetadataWriting/MetadataWriteResult.swift, GPSMetadataEditor.xcodeproj/project.pbxproj</files>
  <read_first>AGENTS.md, GPSMetadataEditor/Features/FileIntake/Models/SelectedMediaFile.swift, GPSMetadataEditor/Features/FileIntake/Models/FileResultStatus.swift, GPSMetadataEditor/Features/FileIntake/Models/GPSStatus.swift, GPSMetadataEditor/Features/CoordinateSelection/Models/CoordinateSelection.swift, .planning/phases/03-core-metadata-writing/03-CONTEXT.md, .planning/phases/03-core-metadata-writing/03-RESEARCH.md</read_first>
  <behavior>
    - Testable contract: a fake writer can return success, warning, and failure results without launching ExifTool.
    - Result values carry `fileID` or `url`, `FileResultStatus`, user-facing `message`, optional diagnostic detail, and optional resulting `GPSStatus`.
    - Writer inputs use existing `SelectedMediaFile` and `CoordinateSelection` value types.
  </behavior>
  <action>Create `GPSMetadataEditor/Features/MetadataWriting/`. Add `MetadataWriter: Sendable` with one async method that writes a `CoordinateSelection` to one `SelectedMediaFile` and returns a non-throwing `MetadataWriteResult`. Add `MetadataWriteResult` as `Equatable` and `Sendable`, with enough fields to update `SelectedMediaFile.latestResult`, `latestMessage`, and `gpsStatus`. Include static helpers or factories only if they remove duplication in tests. Do not import SwiftUI, SwiftData, MapKit, AppKit, or UIKit in these service/result files.</action>
  <verify><automated>xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test</automated></verify>
  <acceptance_criteria>
    - `MetadataWriter.swift` contains `protocol MetadataWriter: Sendable`.
    - `MetadataWriter.swift` contains an `async` write method that returns `MetadataWriteResult`.
    - `MetadataWriteResult.swift` conforms to `Equatable` and `Sendable`.
    - `MetadataWriteResult.swift` references `FileResultStatus` for success/warning/failure mapping.
    - New app source files are listed in `GPSMetadataEditor.xcodeproj/project.pbxproj`.
  </acceptance_criteria>
  <done>Metadata writing has a stable fakeable contract and structured result type.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ExifTool GPS argument builder</name>
  <files>GPSMetadataEditor/Features/MetadataWriting/ExifToolArgumentBuilder.swift, GPSMetadataEditorTests/ExifToolArgumentBuilderTests.swift, GPSMetadataEditor.xcodeproj/project.pbxproj</files>
  <read_first>GPSMetadataEditor/Features/CoordinateSelection/Models/CoordinateSelection.swift, GPSMetadataEditor/Features/FileIntake/Models/SelectedMediaFile.swift, GPSMetadataEditor/Features/FileIntake/Models/MediaFileKind.swift, .planning/phases/03-core-metadata-writing/03-RESEARCH.md, .planning/phases/03-core-metadata-writing/03-PATTERNS.md</read_first>
  <behavior>
    - Test 1: JPEG arguments are exactly `-overwrite_original`, `-gpsposition=<lat>, <lon>`, and the file path as a separate final argument.
    - Test 2: HEIC uses the same GPSPosition argument strategy.
    - Test 3: negative latitude/longitude values remain signed in the GPSPosition argument.
    - Test 4: filenames with spaces, Unicode, and shell metacharacters remain one path argument and are not quoted into a command string.
    - Test 5: MOV/MP4 are not accepted as writable argument targets in Phase 3.
  </behavior>
  <action>Create `ExifToolArgumentBuilder: Sendable` with a method that returns ordered `[String]` arguments for JPEG/HEIC writes only. The argument array must include `-overwrite_original`, `-gpsposition=<latitude>, <longitude>`, and the file path as its own final string. Use `CoordinateSelection.latitude` and `.longitude` values directly; do not create shell-escaped strings, do not include `/bin/sh`, `zsh`, `env`, `exiftool`, or `PATH`, and do not add `_original` backup behavior. Add Swift Testing coverage for exact values and path safety.</action>
  <verify><automated>xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test</automated></verify>
  <acceptance_criteria>
    - `ExifToolArgumentBuilder.swift` contains `-overwrite_original`.
    - `ExifToolArgumentBuilder.swift` contains `-gpsposition=` and does not contain `GPSLatitudeRef` unless implemented as a tested fallback.
    - `ExifToolArgumentBuilderTests.swift` asserts the path is one separate array element for a filename containing spaces and Unicode.
    - `ExifToolArgumentBuilderTests.swift` asserts MOV and MP4 do not produce write arguments.
    - No metadata-writing source contains `/bin/sh`, `zsh`, `/usr/bin/env`, or `ProcessInfo.processInfo.environment`.
  </acceptance_criteria>
  <done>ExifTool GPS write arguments are deterministic, destructive only by explicit flag, and unit-tested without launching ExifTool.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Selected file URL to ExifTool arguments | User-selected file paths may contain spaces, Unicode, and shell metacharacters. |
| Coordinate selection to metadata tags | User-selected coordinates become GPS metadata values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering/Elevation | argument construction | mitigate | Build `[String]` arguments only; keep the path as its own argument and avoid shell command strings. |
| T-03-01-02 | Tampering | destructive overwrite flag | mitigate | Include `-overwrite_original` only in the explicit write argument builder, with confirmation enforced by a later plan before use. |
| T-03-01-03 | Spoofing | unsupported media kind | mitigate | Builder accepts JPEG/HEIC only; MOV/MP4 are warning results in the batch layer. |
| T-03-01-SC | Supply chain | third-party packages | accept | No new Swift package or third-party framework is introduced. |
</threat_model>

<verification>
Run `xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test`.
</verification>

<success_criteria>
- META-01 and META-02 have a tested GPS argument strategy for JPEG/HEIC.
- META-06 is protected by argument-array tests and no shell-string construction.
- META-07 has a structured result type ready for writer and UI integration.
- BATCH-05 destructive overwrite is represented by `-overwrite_original` but not launched before confirmation wiring.
</success_criteria>

<output>
Create `.planning/phases/03-core-metadata-writing/03-01-SUMMARY.md` when done.
</output>
