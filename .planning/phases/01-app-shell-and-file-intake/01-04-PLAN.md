---
phase: 01-app-shell-and-file-intake
plan: 01-04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - GPSMetadataEditor/Features/FileIntake/Views/FileDropZone.swift
  - GPSMetadataEditor/Features/FileIntake/Views/SelectedFilesTable.swift
  - GPSMetadataEditor/Features/FileIntake/Views/FileDetailPanel.swift
  - GPSMetadataEditor/Features/FileIntake/Views/WarningSummaryView.swift
  - GPSMetadataEditor/Features/FileIntake/Views/ReservedLocationPanel.swift
  - GPSMetadataEditor/Features/FileIntake/FileIntakeView.swift
  - GPSMetadataEditorTests/FileIntakeViewModelTests.swift
autonomous: true
requirements:
  - FILE-01
  - FILE-02
  - FILE-03
  - FILE-04
  - FILE-05
must_haves:
  truths:
    - "D-12: Selected rows show display name, type badge, GPS status, and latest result."
    - "D-13/D-14/D-17: Bottom-left detail area shows selected filename and containing folder; right side is not used for file details."
    - "D-15/D-16: GPS status model is scaffolded but accepted rows honestly show `Not checked` until metadata reading exists."
    - "D-18/D-19: Warning surfaces include transient notice plus persistent latest-event details listing every rejected item."
    - "User can complete the Phase 1 intake workflow through picker or drag/drop and review accepted files plus warnings."
  artifacts:
    - path: "GPSMetadataEditor/Features/FileIntake/Views/SelectedFilesTable.swift"
      provides: "selected-file table columns"
    - path: "GPSMetadataEditor/Features/FileIntake/Views/FileDropZone.swift"
      provides: "large and compact drop surfaces"
    - path: "GPSMetadataEditor/Features/FileIntake/Views/WarningSummaryView.swift"
      provides: "latest warning summary and rejected item list"
    - path: "GPSMetadataEditor/Features/FileIntake/Views/FileDetailPanel.swift"
      provides: "selected file details"
  key_links:
    - from: "FileIntakeView.swift"
      to: "SelectedFilesTable.swift"
      via: "passes selected files and selection binding"
      pattern: "SelectedFilesTable"
    - from: "SelectedFilesTable.swift"
      to: "FileIntakeViewModel.swift"
      via: "selected row and snapshot state"
      pattern: "SelectedMediaFile"
    - from: "WarningSummaryView.swift"
      to: "FileIntakeViewModel.swift"
      via: "latest warning details"
      pattern: "IntakeWarning"
---

<objective>
Complete the Phase 1 file intake and review interface.

Purpose: The phase is not done until accepted files, details, and rejected-item warnings are visible in the native utility window.
Output: Extracted SwiftUI subviews for drop surfaces, selected-file table, details, warnings, and the reserved right-side panel.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-app-shell-and-file-intake/01-CONTEXT.md
@.planning/phases/01-app-shell-and-file-intake/01-RESEARCH.md
@.planning/phases/01-app-shell-and-file-intake/01-UI-SPEC.md
@.planning/phases/01-app-shell-and-file-intake/01-PATTERNS.md
@.planning/phases/01-app-shell-and-file-intake/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract complete file intake surfaces</name>
  <files>GPSMetadataEditor/Features/FileIntake/Views/FileDropZone.swift, GPSMetadataEditor/Features/FileIntake/Views/SelectedFilesTable.swift, GPSMetadataEditor/Features/FileIntake/Views/FileDetailPanel.swift, GPSMetadataEditor/Features/FileIntake/Views/WarningSummaryView.swift, GPSMetadataEditor/Features/FileIntake/Views/ReservedLocationPanel.swift, GPSMetadataEditor/Features/FileIntake/FileIntakeView.swift</files>
  <read_first>GPSMetadataEditor/Features/FileIntake/FileIntakeView.swift, GPSMetadataEditor/Features/FileIntake/FileIntakeViewModel.swift, .planning/phases/01-app-shell-and-file-intake/01-UI-SPEC.md, AGENTS.md</read_first>
  <action>Move meaningful SwiftUI regions into separate `View` structs instead of computed view properties. Implement `FileDropZone` with large empty-state and compact-strip modes using the UI-SPEC copy. Implement `SelectedFilesTable` with columns `Display Name`, `Type`, `GPS`, and `Latest Result`, using type badges for JPEG/HEIC/MOV/MP4 and `Not checked` for Phase 1 GPS state. Implement `FileDetailPanel` in the bottom-left region with selected filename, containing folder, access/warning detail, and no full-path table column. Implement `WarningSummaryView` with latest-event warning details and every rejected item from D-19. Implement `ReservedLocationPanel` with quiet Phase 1 copy only. Preserve D-14 by keeping selected-file details out of the right-side panel.</action>
  <verify><automated>xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' build</automated></verify>
  <done>Each listed SwiftUI region lives in its own file; selected file rows expose display name, type, GPS, and latest result; right-side panel contains no file details, map controls, coordinate fields, metadata controls, or batch controls.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Phase 1 behavior and project-rule compliance</name>
  <files>GPSMetadataEditor/Features/FileIntake/FileIntakeView.swift, GPSMetadataEditor/Features/FileIntake/Views/FileDropZone.swift, GPSMetadataEditor/Features/FileIntake/Views/SelectedFilesTable.swift, GPSMetadataEditor/Features/FileIntake/Views/FileDetailPanel.swift, GPSMetadataEditor/Features/FileIntake/Views/WarningSummaryView.swift, GPSMetadataEditor/Features/FileIntake/Views/ReservedLocationPanel.swift, GPSMetadataEditorTests/FileIntakeViewModelTests.swift</files>
  <read_first>AGENTS.md, GPSMetadataEditor/Features/FileIntake/FileIntakeViewModel.swift, GPSMetadataEditorTests/FileIntakeViewModelTests.swift, .planning/phases/01-app-shell-and-file-intake/01-CONTEXT.md</read_first>
  <action>Add or adjust Swift Testing coverage for view-model state that powers the final UI: accepted file count, selected detail content, latest warning replacement, and rejected-item listing. Source-check the implementation for project-forbidden SwiftUI patterns and Phase 1 scope boundaries. Keep all user-facing strings from the UI-SPEC or model-derived filenames/reasons; do not add coordinate selection, metadata reading/writing, batch execution, persistence, folder recursion, or packaging behavior.</action>
  <verify><automated>xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test</automated></verify>
  <done>`xcodebuild ... test` exits 0; source contains no `ObservableObject`, `@Published`, `@StateObject`, `@ObservedObject`, `foregroundColor`, `.cornerRadius`, `onTapGesture`, `DispatchQueue.main.async`, `UIScreen.main.bounds`, `AnyView`, `DateFormatter`, `NumberFormatter`, `Map(`, `SwiftData`, or metadata writer type; tests prove latest warning replacement and selected detail content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Intake state to visual UI | Accepted and rejected file state becomes user-visible copy and table rows. |
| Accessibility tools to app controls | VoiceOver, keyboard, and Voice Control users interact with the same intake controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Spoofing | GPS status display | mitigate | Display `Not checked` for accepted files until metadata reading exists; do not imply GPS metadata was read. |
| T-01-04-02 | Information Disclosure | selected file details | mitigate | Show filename and containing folder in details, not full paths in every row. |
| T-01-04-03 | Denial of Service | long/Unicode filenames | mitigate | Ensure rows truncate safely and full filename remains available through selection/detail state. |
| T-01-04-SC | Tampering | package installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run `xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test`.
</verification>

<success_criteria>
- FILE-01 and FILE-02 are visible and wired through picker/drop surfaces.
- FILE-03 table columns are present with honest Phase 1 GPS/result states.
- FILE-04 warnings are visible, persistent until next intake event, and list every rejected bulk item.
- FILE-05 is covered by service and view-model tests preserving URLs with spaces and Unicode.
- D-01 through D-21 are all represented without implementing deferred folder recursion.
</success_criteria>

<output>
Create `.planning/phases/01-app-shell-and-file-intake/01-04-SUMMARY.md` when done.
</output>
