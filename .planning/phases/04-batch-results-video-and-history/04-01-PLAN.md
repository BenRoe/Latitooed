---
phase: 04-batch-results-video-and-history
plan: 04-01
type: execute
wave: 1
depends_on: []
files_modified:
  - GPSMetadataEditor/Features/FileIntake/FileIntakeView.swift
  - GPSMetadataEditor/Features/FileIntake/FileIntakeViewModel.swift
  - GPSMetadataEditor/Features/FileIntake/Models/SelectedMediaFile.swift
  - GPSMetadataEditor/Features/FileIntake/Views/FileDetailPanel.swift
  - GPSMetadataEditorTests/FileIntakeViewModelTests.swift
  - GPSMetadataEditorTests/MetadataBatchViewModelTests.swift
autonomous: true
requirements:
  - BATCH-02
  - BATCH-03
  - BATCH-04
must_haves:
  truths:
    - "D-01: Do not add active cancellation in Phase 4."
    - "D-02: Do not defer cancellation as future work; treat cancellation as intentionally dropped from product scope for now."
    - "D-03: Do not add a Cancel button, cancellation command, or cancelled file state."
    - "D-04: Rows update only after each file completes."
    - "D-05: The footer shows the currently writing filename and count while the sequential batch runs."
    - "D-06: Footer progress copy is filename-first: `Writing IMG_2042.HEIC (3 of 12)`."
    - "D-07: Keep the existing selected-files table as the primary result surface after a batch."
    - "D-08: Selecting a row shows that file's result message in the bottom-left detail panel."
    - "D-09: Warning and failure rows may expose collapsed technical diagnostics."
    - "D-10: Success rows stay quiet and do not expose technical diagnostics."
    - "D-11: Completed batch summaries remain counts-only, for example `9 updated, 2 warnings, 1 failed.`"
  artifacts:
    - path: "GPSMetadataEditor/Features/FileIntake/FileIntakeViewModel.swift"
      provides: "main-actor batch progress state and selected-row diagnostic detail"
    - path: "GPSMetadataEditor/Features/FileIntake/Views/FileDetailPanel.swift"
      provides: "collapsed diagnostics for selected warning/failure rows"
    - path: "GPSMetadataEditor/Features/FileIntake/FileIntakeView.swift"
      provides: "filename-first footer progress rendering without cancellation UI"
  key_links:
    - from: "FileIntakeViewModel.swift"
      to: "FileIntakeView.swift"
      via: "currentMetadataBatchProgress passed into FileIntakeFooter"
      pattern: "Writing {filename} ({index} of {total})"
---

<objective>
Add transparent sequential batch progress and selected-row diagnostics without reintroducing cancellation.

Purpose: Phase 4 needs better daily-use visibility while preserving the existing table/detail/footer surfaces.
Output: progress value state, filename-first footer copy, diagnostic detail carried to selected rows, and tests proving no cancellation-facing API or UI is added.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-batch-results-video-and-history/04-CONTEXT.md
@.planning/phases/04-batch-results-video-and-history/04-RESEARCH.md
@.planning/phases/04-batch-results-video-and-history/04-UI-SPEC.md
@.planning/phases/04-batch-results-video-and-history/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish filename-first batch progress</name>
  <files>GPSMetadataEditor/Features/FileIntake/FileIntakeViewModel.swift, GPSMetadataEditor/Features/FileIntake/FileIntakeView.swift, GPSMetadataEditorTests/MetadataBatchViewModelTests.swift</files>
  <read_first>GPSMetadataEditor/Features/FileIntake/FileIntakeViewModel.swift, GPSMetadataEditor/Features/FileIntake/FileIntakeView.swift, GPSMetadataEditor/Features/FileIntake/Models/SelectedMediaFile.swift, GPSMetadataEditor/Features/MetadataWriting/MetadataWriter.swift, .planning/phases/04-batch-results-video-and-history/04-CONTEXT.md, .planning/phases/04-batch-results-video-and-history/04-UI-SPEC.md</read_first>
  <behavior>
    - Before each awaited writer call, progress contains that file's display name, 1-based index, and total count.
    - Rows are still replaced only after each awaited writer call returns.
    - Progress clears after the batch finishes, including when the writer returns warning or failure results.
    - The footer renders `Writing IMG_2042.HEIC (3 of 12)` style copy while running.
  </behavior>
  <action>Add `MetadataBatchProgress: Equatable, Sendable` or equivalent to `FileIntakeViewModel` with `filename`, `currentIndex`, `totalCount`, and a computed display string exactly shaped as `Writing \(filename) (\(currentIndex) of \(totalCount))`. Add `currentMetadataBatchProgress` state. In `applyMetadata(coordinate:writer:)`, set progress immediately before `await writer.writeGPS(coordinate, to: file)` using 1-based indexes from the existing sequential `for` loop, then keep the existing row replacement after the await. Clear progress in `defer` alongside `isMetadataBatchRunning = false`. Pass the progress value into `FileIntakeFooter` and prefer the progress display string over the generic running copy. Do not add a Cancel button, `cancelBatch()`, `.cancelled`, progress percent, parallel task group, per-file `Task {}`, or new progress panel.</action>
  <verify><automated>xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test</automated></verify>
  <acceptance_criteria>
    - `FileIntakeViewModel.swift` contains `currentMetadataBatchProgress`.
    - A test observes progress display text `Writing IMG_001.HEIC (1 of 2)` before the first fake writer resumes.
    - A test proves a selected row remains pending while the fake writer is suspended and updates only after the writer returns.
    - `FileIntakeView.swift` footer running text uses the progress value when it is non-nil.
    - `GPSMetadataEditor/Features/FileIntake` contains no `Cancel`, `cancelBatch`, or `cancelled` user-facing batch API added by this plan.
  </acceptance_criteria>
  <done>Active batch progress is visible in the existing footer without adding cancellation controls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry diagnostic detail into selected-file detail</name>
  <files>GPSMetadataEditor/Features/FileIntake/FileIntakeViewModel.swift, GPSMetadataEditor/Features/FileIntake/Models/SelectedMediaFile.swift, GPSMetadataEditorTests/FileIntakeViewModelTests.swift, GPSMetadataEditorTests/MetadataBatchViewModelTests.swift</files>
  <read_first>GPSMetadataEditor/Features/FileIntake/FileIntakeViewModel.swift, GPSMetadataEditor/Features/FileIntake/Models/SelectedMediaFile.swift, GPSMetadataEditor/Features/MetadataWriting/MetadataWriteResult.swift, GPSMetadataEditor/Features/FileIntake/Models/FileResultStatus.swift, .planning/phases/04-batch-results-video-and-history/04-CONTEXT.md</read_first>
  <behavior>
    - `MetadataWriteResult.diagnosticDetail` is not discarded during row replacement.
    - Selected warning/failure rows expose diagnostic detail through `selectedFileDetail`.
    - Selected success rows do not expose diagnostic detail even if the process produced stdout/stderr.
  </behavior>
  <action>Add optional diagnostic storage to the selected-row path, preferably `latestDiagnosticDetail` on `SelectedMediaFile` and `SelectedFileDetail`. Update `replaceSelectedFile(matching:)` to copy `result.diagnosticDetail` only when `result.status` is `.warning` or `.failure`; set it to `nil` for `.success` and preserve the existing `latestMessage` behavior. Keep `SelectedMediaFile` immutable, `Hashable`, and `Sendable`. Do not persist diagnostics and do not add diagnostics to batch history models.</action>
  <verify><automated>xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test</automated></verify>
  <acceptance_criteria>
    - `SelectedMediaFile.swift` has an optional diagnostic detail field or the view model has an ID-keyed diagnostic detail map.
    - `FileIntakeViewModel.SelectedFileDetail` exposes optional diagnostic detail.
    - Tests assert warning and failure selected details include diagnostic strings.
    - Tests assert success selected details have `nil` diagnostic detail.
    - No SwiftData model or new persistence type stores diagnostic detail.
  </acceptance_criteria>
  <done>Selected warning/failure rows retain diagnostics for review, while success rows remain quiet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render collapsed diagnostics in the detail panel</name>
  <files>GPSMetadataEditor/Features/FileIntake/Views/FileDetailPanel.swift, GPSMetadataEditorTests/FileIntakeSmokeTests.swift</files>
  <read_first>GPSMetadataEditor/Features/FileIntake/Views/FileDetailPanel.swift, GPSMetadataEditor/Features/FileIntake/FileIntakeViewModel.swift, GPSMetadataEditor/Features/FileIntake/Models/FileResultStatus.swift, .planning/phases/04-batch-results-video-and-history/04-UI-SPEC.md, .agents/skills/swiftui-pro/references/accessibility.md</read_first>
  <behavior>
    - The detail panel shows selected filename, folder, latest result, and latest user-facing message as before.
    - Warning/failure selected rows with diagnostic detail show a collapsed native disclosure labeled `Diagnostics`.
    - Success rows and rows without diagnostic detail show no diagnostic disclosure.
    - Diagnostic text is selectable.
  </behavior>
  <action>Update `FileDetailPanel` or a new focused subview under `GPSMetadataEditor/Features/FileIntake/Views/` to render a compact `DisclosureGroup("Diagnostics")` only when `detail.latestResult` is `.warning` or `.failure` and diagnostic detail is non-empty. Place the disclosure below the user-facing latest message in the existing bottom-left panel. Apply `.textSelection(.enabled)` to the diagnostic text. Keep SwiftUI text styles dynamic; do not use fixed font sizes, `foregroundColor`, `.cornerRadius`, `onTapGesture`, `AnyView`, or UIKit colors.</action>
  <verify><automated>xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test</automated></verify>
  <acceptance_criteria>
    - `FileDetailPanel.swift` contains `DisclosureGroup("Diagnostics")` or an equivalent native collapsed disclosure.
    - Diagnostic text uses `.textSelection(.enabled)`.
    - Source checks find no `foregroundColor`, `.cornerRadius`, `onTapGesture`, or `AnyView` in the changed detail panel code.
    - A smoke test or view-model test proves success detail has no diagnostic text available for rendering.
  </acceptance_criteria>
  <done>The existing detail panel supports focused warning/failure diagnostics without changing the primary result surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Helper diagnostics to UI | ExifTool stdout/stderr may contain technical details and file paths. |
| Batch execution state to UI | Progress state must describe the active write without adding user cancellation promises. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | diagnostic detail panel | mitigate | Show raw diagnostics only in collapsed warning/failure detail, never in success rows or history. |
| T-04-01-02 | Repudiation | batch progress | mitigate | Publish current filename and 1-based count before each write so the footer reflects active work. |
| T-04-01-03 | Scope control | cancellation | mitigate | Cover BATCH-02 by preserving no public cancellation UI/state per D-01 through D-03. |
| T-04-01-SC | Supply chain | dependencies | accept | No third-party package or framework is introduced. |
</threat_model>

<verification>
Run `xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test`.
</verification>

<success_criteria>
- BATCH-02 is covered by explicit omission of cancellation UI/state per the Phase 4 context override.
- BATCH-03 is satisfied by filename-first footer progress while a sequential batch is running.
- BATCH-04 is satisfied by row-level success/warning/failure results plus selected-row diagnostics for warning/failure cases.
</success_criteria>

<output>
Create `.planning/phases/04-batch-results-video-and-history/04-01-SUMMARY.md` when done.
</output>
