---
phase: "09.1"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - GPSMetadataEditor/Features/FileIntake/FileIntakeViewModel.swift
  - GPSMetadataEditor/Features/CoordinateSelection/Services/CoordinateSearchService.swift
  - GPSMetadataEditorTests/ProcessRunnerTests.swift
  - GPSMetadataEditorTests/CoordinateSelectionViewModelTests.swift
  - GPSMetadataEditorTests/FileIntakeViewModelTests.swift
  - GPSMetadataEditorTests/MetadataBatchViewModelTests.swift
autonomous: true
requirements: []

must_haves:
  truths:
    - "GPS scan Task is stored as activeGPSScanTask, cancelled before reassignment, and cancelled in deinit"
    - "waitUntil in MetadataBatchViewModelTests uses Task.yield() not Task.sleep(), with #_sourceLocation"
    - "ProcessRunnerTests catches CocoaError specifically, not (any Error)"
    - "CoordinateSelectionViewModelTests boundary tests use @Test(arguments:) parameterized form"
    - "RecordingGPSReadRunner captures arguments to var; assertions moved to test call site"
    - "@unchecked Sendable on MKLocalSearchCompletionBox has an explanatory comment"
  artifacts:
    - path: "GPSMetadataEditor/Features/FileIntake/FileIntakeViewModel.swift"
      provides: "activeGPSScanTask field + deinit"
      contains: "activeGPSScanTask"
    - path: "GPSMetadataEditorTests/MetadataBatchViewModelTests.swift"
      provides: "waitUntil with Task.yield and #_sourceLocation"
      contains: "#_sourceLocation"
    - path: "GPSMetadataEditorTests/ProcessRunnerTests.swift"
      provides: "CocoaError-specific catch"
      contains: "CocoaError"
    - path: "GPSMetadataEditorTests/CoordinateSelectionViewModelTests.swift"
      provides: "parameterized boundary tests"
      contains: "@Test(\"Latitude accepts boundary values\", arguments:"
  key_links:
    - from: "FileIntakeViewModel.intake(urls:source:)"
      to: "activeGPSScanTask"
      via: "cancel-before-assign + Task { await refreshGPSStatuses(for:) }"
      pattern: "activeGPSScanTask\\?"
---

<objective>
Fix all Swift Concurrency and Swift Testing HIGH/MEDIUM/LOW findings from the audit.

Purpose: Eliminate a Task leak in FileIntakeViewModel (untracked GPS scan task), fix
sleep-based test polling, tighten error-type assertions in ProcessRunnerTests, convert
for-loop parameterized tests to @Test(arguments:), move #expect calls out of a helper
type to test call sites, and add a clarifying comment on @unchecked Sendable.

Output: 6 source files changed. All changes are correctness-only — no UI or data model
touches. The test suite must continue to pass after each task.
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-swift-skills-code-quality/09.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Store and cancel GPS scan Task in FileIntakeViewModel</name>
  <files>GPSMetadataEditor/Features/FileIntake/FileIntakeViewModel.swift</files>

  <read_first>
    GPSMetadataEditor/Features/FileIntake/FileIntakeViewModel.swift — full file (already read; line 156-163 is the bare Task {} that must be wrapped)
  </read_first>

  <action>
    In FileIntakeViewModel (an @Observable @MainActor final class):

    1. After the two existing @ObservationIgnored fields (lines 139-142), add a third:
       @ObservationIgnored
       private var activeGPSScanTask: Task<Void, Never>?

    2. In intake(urls:source:) (line 156), replace the bare Task block (lines 161-163) with:
       activeGPSScanTask?.cancel()
       activeGPSScanTask = Task {
           await refreshGPSStatuses(for: acceptedFiles)
       }

    3. After the closing brace of init (line 150), add a deinit:
       deinit {
           activeGPSScanTask?.cancel()
       }

    Rationale: @ObservationIgnored prevents the task handle from triggering view updates.
    deinit is safe on an @Observable ARC-managed class — Task.cancel() is non-async and
    just sets a cooperative cancellation flag. Do not await anything in deinit.
  </action>

  <verify>
    <automated>xcodebuild build -scheme GPSMetadataEditor -destination 'platform=macOS' 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - FileIntakeViewModel.swift contains the literal string "activeGPSScanTask?.cancel()" (at least twice: in intake and deinit)
    - FileIntakeViewModel.swift contains "private var activeGPSScanTask: Task&lt;Void, Never&gt;?"
    - FileIntakeViewModel.swift contains "deinit {"
    - Build succeeds with no new errors
  </acceptance_criteria>

  <done>activeGPSScanTask is declared @ObservationIgnored, cancelled before reassignment in intake(), and cancelled in deinit. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix test quality — ProcessRunnerTests, CoordinateSelectionViewModelTests, FileIntakeViewModelTests, MetadataBatchViewModelTests</name>
  <files>
    GPSMetadataEditorTests/ProcessRunnerTests.swift,
    GPSMetadataEditorTests/CoordinateSelectionViewModelTests.swift,
    GPSMetadataEditorTests/FileIntakeViewModelTests.swift,
    GPSMetadataEditorTests/MetadataBatchViewModelTests.swift,
    GPSMetadataEditor/Features/CoordinateSelection/Services/CoordinateSearchService.swift
  </files>

  <read_first>
    GPSMetadataEditorTests/ProcessRunnerTests.swift — full file (already read; line 7 is the (any Error).self catch)
    GPSMetadataEditorTests/CoordinateSelectionViewModelTests.swift — lines 8-20 (for-loops) and line 297 (isEmpty == true)
    GPSMetadataEditorTests/FileIntakeViewModelTests.swift — lines 363-373 (RecordingGPSReadRunner), lines 398-416 (waitUntil)
    GPSMetadataEditorTests/MetadataBatchViewModelTests.swift — lines 304-322 (waitUntil with Task.sleep)
    GPSMetadataEditor/Features/CoordinateSelection/Services/CoordinateSearchService.swift — line 18 (@unchecked Sendable)
  </read_first>

  <action>
    Make five independent changes. Each is surgical — touch only the specific lines listed.

    CHANGE A — ProcessRunnerTests.swift (line 7):
    Replace the entire test body. The current form uses #expect(throws: (any Error).self) { ... }.
    Replace with a do/catch that catches CocoaError specifically:

        @Test func launchFailureThrowsCocoaError() async {
            do {
                try await FoundationProcessRunner().run(
                    executableURL: URL(filePath: "/tmp/not-a-real-process-\(UUID().uuidString)"),
                    arguments: []
                )
                Issue.record("Expected launch to throw but it did not.")
            } catch is CocoaError {
                // expected — process launch failed with CocoaError
            } catch {
                Issue.record("Unexpected error type: \(error)")
            }
        }

    CHANGE B — CoordinateSelectionViewModelTests.swift (lines 8-23):
    Replace the two for-loop tests with @Test(arguments:) parameterized tests:

        @Test("Latitude accepts boundary values", arguments: [-90.0, 0.0, 90.0])
        func latitudeAcceptsBoundaryValues(value: Double) throws {
            let coordinate = try #require(CoordinateSelection(latitude: value, longitude: 0))
            #expect(coordinate.latitude == value)
        }

        @Test("Longitude accepts boundary values", arguments: [-180.0, 0.0, 180.0])
        func longitudeAcceptsBoundaryValues(value: Double) throws {
            let coordinate = try #require(CoordinateSelection(latitude: 0, longitude: value))
            #expect(coordinate.longitude == value)
        }

    Also on line 297, change:
        #expect(viewModel.searchResults.isEmpty == true)
    to:
        #expect(viewModel.searchResults.isEmpty)

    CHANGE C — FileIntakeViewModelTests.swift — RecordingGPSReadRunner (lines 363-373):
    The struct RecordingGPSReadRunner currently calls #expect inside run(). Add a var
    capturedArguments: [String] = [] property (needs to be var, so make it a class or
    use an actor; simplest: keep struct but add capturedArguments as a mutating concern
    — but since ProcessRunning protocol requires func not mutating func, use a class instead).

    Replace the private struct RecordingGPSReadRunner with a private final class:

        private final class RecordingGPSReadRunner: ProcessRunning, @unchecked Sendable {
            let result: ProcessResult
            private(set) var capturedArguments: [String] = []

            init(result: ProcessResult) { self.result = result }

            func run(executableURL: URL, arguments: [String]) async throws -> ProcessResult {
                capturedArguments = arguments
                return result
            }
        }

    Then in the three test methods that construct a RecordingGPSReadRunner and call
    reader.gpsStatus(for:) — each creates a local `let reader = ExifToolGPSMetadataReader(... processRunner: RecordingGPSReadRunner(result: ...) ...)`.
    After the `await reader.gpsStatus(for: file)` call, add assertions:

        let runner = RecordingGPSReadRunner(result: ...)   // store in a local let
        // ... build reader with runner ...
        let status = await reader.gpsStatus(for: file)
        #expect(runner.capturedArguments.contains("-json"))
        #expect(runner.capturedArguments.contains("-n"))
        #expect(runner.capturedArguments.contains("-GPSLatitude"))
        #expect(runner.capturedArguments.contains("-GPSLongitude"))

    The three affected tests are: exifToolGPSMetadataReaderMapsJSONCoordinatesToDisplayableStatus,
    exifToolGPSMetadataReaderMapsMissingCoordinatesToNoCoordinates,
    exifToolGPSMetadataReaderMapsVideoGPSCoordinatesString.

    CHANGE D — FileIntakeViewModelTests.swift — waitUntil (lines 398-416):
    Replace the 4-parameter waitUntil with the #_sourceLocation form:

        private func waitUntil(
            _ condition: @MainActor @escaping () -> Bool,
            sourceLocation: SourceLocation = #_sourceLocation
        ) async throws {
            for _ in 0..<20 {
                if await condition() { return }
                await Task.yield()
            }
            Issue.record("Condition was not met", sourceLocation: sourceLocation)
        }

    CHANGE E — MetadataBatchViewModelTests.swift — waitUntil (lines 304-322):
    Replace the 4-parameter waitUntil (which uses Task.sleep) with the same #_sourceLocation form:

        private func waitUntil(
            _ condition: @MainActor @escaping () -> Bool,
            sourceLocation: SourceLocation = #_sourceLocation
        ) async throws {
            for _ in 0..<20 {
                if await condition() { return }
                await Task.yield()
            }
            Issue.record("Condition was not met", sourceLocation: sourceLocation)
        }

    CHANGE F — CoordinateSearchService.swift — add comment on line 18:
    The line currently reads:
        nonisolated final class MKLocalSearchCompletionBox: @unchecked Sendable {
    Add a comment on the line immediately above it:
        // @unchecked Sendable: immutable let wrapper — MKLocalSearchCompletion is safe to
        // read across actors but not declared Sendable by MapKit.

    Remove the leading 4 spaces before "import Foundation" at line 1 of FileIntakeViewModelTests.swift.
    The file currently opens with "    import Foundation" (4 leading spaces).
  </action>

  <verify>
    <automated>xcodebuild test -scheme GPSMetadataEditor -destination 'platform=macOS' 2>&1 | grep -E "Test Suite|PASSED|FAILED|error:"</automated>
  </verify>

  <acceptance_criteria>
    - ProcessRunnerTests.swift does not contain "(any Error).self"; contains "CocoaError"
    - CoordinateSelectionViewModelTests.swift contains '@Test("Latitude accepts boundary values", arguments:' and '@Test("Longitude accepts boundary values", arguments:'
    - CoordinateSelectionViewModelTests.swift does not contain "for value in [-90" or "for value in [-180"
    - CoordinateSelectionViewModelTests.swift line for isEmpty: "#expect(viewModel.searchResults.isEmpty)" without "== true"
    - FileIntakeViewModelTests.swift contains "capturedArguments" and the assertions at call site
    - FileIntakeViewModelTests.swift waitUntil uses "#_sourceLocation"
    - MetadataBatchViewModelTests.swift waitUntil uses "#_sourceLocation" and "Task.yield()" (not "Task.sleep")
    - CoordinateSearchService.swift line before MKLocalSearchCompletionBox contains "immutable let wrapper"
    - FileIntakeViewModelTests.swift starts with "import Foundation" (no leading spaces)
    - Full test suite passes (xcodebuild test exits 0 / "Test Suite ... passed")
  </acceptance_criteria>

  <done>All 6 test quality changes applied. Test suite passes. waitUntil in both test files uses #_sourceLocation and Task.yield(). RecordingGPSReadRunner captures arguments; assertions moved to call site.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries in this plan. All changes are internal source-only modifications
to production ViewModel and test files. No user input paths, network surface, or IPC
boundaries are affected.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — Apple frameworks only |
</threat_model>

<verification>
After both tasks complete:

1. Build succeeds: `xcodebuild build -scheme GPSMetadataEditor -destination 'platform=macOS'`
2. Full test suite passes: `xcodebuild test -scheme GPSMetadataEditor -destination 'platform=macOS'`
3. FileIntakeViewModel.swift: grep confirms `activeGPSScanTask` present (min 3 occurrences: declaration, cancel in intake, cancel in deinit)
4. MetadataBatchViewModelTests.swift: grep confirms no `Task.sleep` in waitUntil
5. ProcessRunnerTests.swift: grep confirms `CocoaError` present, `(any Error)` absent
</verification>

<success_criteria>
- All CONC-HIGH, CONC-MEDIUM, TEST-HIGH, TEST-MEDIUM, TEST-LOW, CONC-LOW findings addressed
- xcodebuild test exits 0 with no regressions
- No new warnings introduced
</success_criteria>

<output>
Create `.planning/phases/09.1-swift-skills-code-quality/09.1-01-SUMMARY.md` when done.
</output>
