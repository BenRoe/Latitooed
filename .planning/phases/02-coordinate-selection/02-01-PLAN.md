---
phase: 02-coordinate-selection
plan: 02-01
type: execute
wave: 1
depends_on: []
files_modified:
  - GPSMetadataEditor.xcodeproj/project.pbxproj
  - GPSMetadataEditor/Features/CoordinateSelection/CoordinateSelectionViewModel.swift
  - GPSMetadataEditor/Features/CoordinateSelection/Models/CoordinateSelection.swift
  - GPSMetadataEditor/Features/CoordinateSelection/Models/CoordinateFieldState.swift
  - GPSMetadataEditor/Features/CoordinateSelection/Models/MapPresentationStyle.swift
  - GPSMetadataEditorTests/CoordinateSelectionViewModelTests.swift
autonomous: true
requirements:
  - LOC-03
  - LOC-04
  - LOC-05
  - LOC-06
must_haves:
  truths:
    - "D-08: Initial map state defaults to Berlin and has no selected target pin."
    - "D-09: Valid manual latitude and longitude edits update selected coordinate state immediately."
    - "D-10: Invalid latitude or longitude remains editable, shows inline validation state, and does not replace the selected target."
    - "D-11: Coordinate display uses 6 decimal places."
    - "D-12: Manual coordinate entry becomes active source and collapses old search results."
    - "D-15: Coordinate selection supports a dense pro layout with minimal labels and high map-space priority."
    - "D-16: View-model state exposes a selected-coordinate ready status for the right-panel controls."
  artifacts:
    - path: "GPSMetadataEditor/Features/CoordinateSelection/CoordinateSelectionViewModel.swift"
      provides: "main-actor coordinate state, validation, and commands"
    - path: "GPSMetadataEditor/Features/CoordinateSelection/Models/CoordinateSelection.swift"
      provides: "selected coordinate value and 6-decimal display"
    - path: "GPSMetadataEditorTests/CoordinateSelectionViewModelTests.swift"
      provides: "coordinate validation and state transition tests"
  key_links:
    - from: "CoordinateSelectionViewModel.swift"
      to: "CoordinateSelection.swift"
      via: "selected coordinate state"
      pattern: "CoordinateSelection"
    - from: "CoordinateSelectionViewModelTests.swift"
      to: "CoordinateSelectionViewModel.swift"
      via: "@testable import GPSMetadataEditor"
      pattern: "@testable import GPSMetadataEditor"
---

<objective>
Create the coordinate-selection state foundation for Phase 2.

Purpose: The UI and search plans need a tested main-actor state model for selected coordinates, manual latitude/longitude editing, map style selection, Berlin default camera seed, and ready-status text.
Output: Coordinate value types, `@Observable @MainActor` view model, project membership, and Swift Testing coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-coordinate-selection/02-CONTEXT.md
@.planning/phases/02-coordinate-selection/02-RESEARCH.md
@.planning/phases/02-coordinate-selection/02-UI-SPEC.md
@.planning/phases/02-coordinate-selection/02-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add coordinate value types and validation tests</name>
  <files>GPSMetadataEditor/Features/CoordinateSelection/Models/CoordinateSelection.swift, GPSMetadataEditor/Features/CoordinateSelection/Models/CoordinateFieldState.swift, GPSMetadataEditor/Features/CoordinateSelection/Models/MapPresentationStyle.swift, GPSMetadataEditorTests/CoordinateSelectionViewModelTests.swift, GPSMetadataEditor.xcodeproj/project.pbxproj</files>
  <read_first>AGENTS.md, GPSMetadataEditor/Features/FileIntake/Models/SelectedMediaFile.swift, GPSMetadataEditor/Features/FileIntake/Models/GPSStatus.swift, GPSMetadataEditorTests/FileIntakeViewModelTests.swift, .planning/phases/02-coordinate-selection/02-CONTEXT.md, .planning/phases/02-coordinate-selection/02-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: latitude accepts `-90`, `0`, and `90`.
    - Test 2: longitude accepts `-180`, `0`, and `180`.
    - Test 3: latitude rejects values below `-90` and above `90`.
    - Test 4: longitude rejects values below `-180` and above `180`.
    - Test 5: coordinate display formats latitude and longitude with exactly 6 fractional digits.
  </behavior>
  <action>Create a `CoordinateSelection` value type that stores latitude and longitude as `Double`, conforms to `Equatable` and `Sendable`, validates latitude in `-90...90` and longitude in `-180...180`, and exposes 6-decimal display strings. Create `CoordinateFieldState` or equivalent value state for editable latitude/longitude field text/value plus validation message. Create `MapPresentationStyle` for `.standard`, `.satellite`, and `.hybrid`. Add all new app and test files to `GPSMetadataEditor.xcodeproj/project.pbxproj`. Do not add SwiftData, persistence, default-location settings, UIKit/AppKit wrappers, or third-party frameworks.</action>
  <verify><automated>xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test</automated></verify>
  <acceptance_criteria>
    - `CoordinateSelection.swift` contains latitude range `-90...90` and longitude range `-180...180`.
    - `CoordinateSelection.swift` formats coordinate display at 6 fractional digits without `String(format:)`.
    - `MapPresentationStyle.swift` contains cases for standard, satellite, and hybrid.
    - `CoordinateSelectionViewModelTests.swift` contains tests for valid boundaries, invalid ranges, and 6-decimal display.
    - `GPSMetadataEditor.xcodeproj/project.pbxproj` includes new CoordinateSelection model/test files in correct source build phases.
  </acceptance_criteria>
  <done>Coordinate value types compile and tests prove range validation plus 6-decimal display.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add main-actor coordinate selection view model</name>
  <files>GPSMetadataEditor/Features/CoordinateSelection/CoordinateSelectionViewModel.swift, GPSMetadataEditorTests/CoordinateSelectionViewModelTests.swift, GPSMetadataEditor.xcodeproj/project.pbxproj</files>
  <read_first>GPSMetadataEditor/Features/FileIntake/FileIntakeViewModel.swift, GPSMetadataEditor/Features/CoordinateSelection/Models/CoordinateSelection.swift, GPSMetadataEditor/Features/CoordinateSelection/Models/CoordinateFieldState.swift, GPSMetadataEditor/Features/CoordinateSelection/Models/MapPresentationStyle.swift, .planning/phases/02-coordinate-selection/02-RESEARCH.md, .planning/phases/02-coordinate-selection/02-PATTERNS.md</read_first>
  <behavior>
    - Test 1: a new view model has no selected coordinate and exposes Berlin as the default map center.
    - Test 2: valid manual latitude and longitude updates selected coordinate immediately.
    - Test 3: invalid manual latitude or longitude leaves the previous selected coordinate unchanged and exposes the correct inline validation message.
    - Test 4: manual valid edit collapses search results state.
    - Test 5: selected-coordinate ready status returns `No target coordinate selected.` before selection and `Target set: {latitude}, {longitude}` after selection.
  </behavior>
  <action>Create `@Observable @MainActor final class CoordinateSelectionViewModel`. Model state must include selected coordinate, editable latitude/longitude field state, selected map style, search query text, whether results are expanded, inline status text, and Berlin default map seed. Add commands for setting coordinate from manual input, setting coordinate from map click, collapsing search results while preserving query text, and changing map style. Mirror `FileIntakeViewModel` patterns: service dependencies ignored by observation when present, no `ObservableObject`, no `@Published`, no `DispatchQueue.main.async`, no `Task.detached`, and no SwiftData.</action>
  <verify><automated>xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test</automated></verify>
  <acceptance_criteria>
    - `CoordinateSelectionViewModel.swift` is annotated `@Observable` and `@MainActor`.
    - New view-model tests assert Berlin default, nil initial selection, valid manual update, invalid edit preservation, result collapse, and ready status copy.
    - `CoordinateSelectionViewModel.swift` contains no `ObservableObject`, `@Published`, `@StateObject`, `@ObservedObject`, `DispatchQueue.main.async`, `Task.detached`, or SwiftData imports.
    - `xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test` exits 0 on host.
  </acceptance_criteria>
  <done>Coordinate selection state is testable, main-actor isolated, and ready for MapKit search/UI integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User coordinate input to app state | Manual latitude/longitude text is user-controlled and can be invalid or partial. |
| View-model state to later metadata writer | Phase 2 selected coordinate will become Phase 3 batch-write input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | manual coordinate input | mitigate | Validate latitude and longitude ranges before replacing selected target state. |
| T-02-01-02 | Spoofing | ready status | mitigate | Show explicit `No target coordinate selected.` until a real coordinate exists; do not create a fake Berlin target pin. |
| T-02-01-03 | Information Disclosure | coordinate defaults | accept | Berlin default is non-user-specific and not derived from location permission. |
| T-02-01-SC | Tampering | package installs | accept | No third-party packages or registries are planned. |
</threat_model>

<verification>
Run `xcodebuild -project GPSMetadataEditor.xcodeproj -scheme GPSMetadataEditor -destination 'platform=macOS' test`.
</verification>

<success_criteria>
- LOC-05 is supported by validated manual latitude/longitude state.
- LOC-06 is supported by selected-coordinate ready status.
- D-08 through D-12 and D-15 through D-16 are represented in tested state.
</success_criteria>

<output>
Create `.planning/phases/02-coordinate-selection/02-01-SUMMARY.md` when done.
</output>
